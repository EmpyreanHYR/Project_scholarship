# 奖学金评审软件依赖
# 基础依赖（必需）
pandas>=1.5.0
openpyxl>=3.0.0
Pillow>=9.0.0

# 可视化（可选）
matplotlib>=3.5.0

# PDF 报告导出（可选）
reportlab>=3.6.0

# 数据库支持（可选，v3.0新增）
SQLAlchemy>=1.4.0

# 数据导入增强（可选，v3.1新增）
chardet>=4.0.0
